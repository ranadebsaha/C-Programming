#include<stdio.h>
#include<stdlib.h>
struct node{
    int data;
    struct node *link;
};
void maxChocolates(struct node*);

void main(){
  struct node *p;
    p=NULL;

    void maxChocolates(p);
}

void maxChocolates(struct node* head){
    struct node* temp = head;
    struct node* ptr= head;
do{
    while(ptr->link!=NULL){
    ptr=ptr->link;
    }
    if()
    temp=temp->link;
}while(temp->link!=NULL)
   
    int 
}